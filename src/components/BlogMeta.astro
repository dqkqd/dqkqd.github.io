---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <div class="flex flex-wrap gap-4 pt-8 text-sm">
      {tags.map((tag) => (
        <a href={`/tags/${tag}`} class="link">
          #{tag}
        </a>
      ))}
    </div>
  )
}
